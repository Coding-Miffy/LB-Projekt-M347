apiVersion: apps/v1
kind: Deployment
metadata:
  name: mariadb-deployment
  namespace: m347-wordpress
  labels:
    app: mariadb-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mariadb-app
  template:
    metadata:
      labels:
        app: mariadb-app
    spec:
      containers:
      - name: mariadb-container
        image: mariadb:latest
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: secret
              key: root_password
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: configmap
              key: database_name
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: secret
              key: username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: secret
              key: password
        volumeMounts:
        - name: mariadb-storage-volume
          mountPath: /var/lib/mysql
      volumes:
      - name: mariadb-storage-volume
        persistentVolumeClaim:
          claimName: mariadb-storage-pvc
